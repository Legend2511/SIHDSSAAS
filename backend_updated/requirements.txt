# WeatherGPT Backend - SIH 2026 Hackathon MVP

# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic-settings==2.5.2
python-dotenv==1.0.1

# LLM
groq==0.11.0

# Embeddings — we use sentence-transformers for local embeddings
# and store vectors in a simple JSON-based local store to avoid
# the heavy chromadb/onnxruntime/cmake dependency chain
sentence-transformers==3.1.0

# HTTP client
httpx==0.27.2

# Translation
deep-translator==1.11.4

# WhatsApp (Twilio)
twilio==9.3.0

# Database
aiosqlite==0.20.0

# Auth (Phase 7)
PyJWT==2.9.0
email-validator==2.2.0

# Utilities
python-multipart==0.0.9

# SSL cert fix for MSYS2 Python
pip-system-certs==4.0
certifi
